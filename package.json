{
  "name": "careeroptic-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "npm --prefix backend run build",
    "build": "npm --prefix frontend run build && rm -rf dist docs assets && cp -r frontend/dist dist && cp -r frontend/dist docs && cp -r frontend/dist/assets assets && cp frontend/dist/index.html ./index.html && cp frontend/dist/404.html ./404.html && cp frontend/dist/.nojekyll ./.nojekyll",
    "start": "npm --prefix backend start"
  }
}
